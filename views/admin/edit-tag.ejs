<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Etiqueta</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>

    <%- include('menu_top_admin') %>


    <div class="container">
        <h2>Editar Etiqueta</h2>
        <form action="/dashboard/tags/edit-tag/<%= tag._id %>?_method=PUT" method="POST">
            <input type="hidden" name="_method" value="PUT">

            <label for="name">Nombre de la Etiqueta:</label>
            <input type="text" name="name" id="name" value="<%= tag.name %>" required>

            <label for="description">Descripción:</label>
            <textarea name="description" id="description"><%= tag.description || '' %></textarea>

            <button type="submit" class="btn">Actualizar Etiqueta</button>
        </form>

        <a href="/dashboard/tags" class="back-link">← Volver a Tags</a>
    </div>

</body>
</html>
