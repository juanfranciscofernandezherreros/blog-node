<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Usuarios Dashboard</title>
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>

  <%- include('menu_top_admin') %>

  <div class="container">
    <div class="admin-title">
      <h2>Gestión de Usuarios</h2>
      <a href="/dashboard/users/add-user" class="button">+ Añadir Nuevo Usuario</a>
    </div>

    <% if (successMessage) { %>
      <div class="success-message"><%= successMessage %></div>
    <% } %>

    <table class="admin-table">
      <thead>
        <tr>
          <th>Nombre de Usuario</th>
          <th>Email</th>
          <th>Roles</th>
          <th>Posts</th>
          <th>Fecha de Creación</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% data.forEach(user => { %>
          <tr>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td>
              <% user.roles.forEach(role => { %>
                <span class="role-badge"><%= role.name %></span>
              <% }) %>
            </td>
            <td><%= user.posts ? user.posts.length : 0 %></td>
            <td><%= new Date(user.createdAt).toLocaleDateString() %></td>
            <td>
              <a href="/dashboard/users/edit-user/<%= user._id %>" class="btn btn-edit">Editar</a>
              <form action="/dashboard/users/delete-user/<%= user._id %>" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-delete" onclick="return confirm('¿Estás seguro de eliminar este usuario?');">Eliminar</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>

</body>
</html>
